<<<<<<< HEAD
# 数据分析实战练习项目：电子产品销售分析

这是一个专为数据分析初学者设计的实战练习项目，旨在帮助您熟悉和掌握 NumPy、Pandas 和 Matplotlib 这三个核心 Python 库在数据分析中的应用。

## 1. 项目目标
- 数据清洗与预处理：学习如何使用 Pandas 加载数据、检查数据类型、处理日期格式。
- 描述性统计：利用 Pandas 和 NumPy 计算关键业务指标（如总销售额、平均订单价值）。
- 数据可视化：使用 Matplotlib 绘制不同维度的图表，直观展示数据洞察。
- 业务分析：通过时间、类别和价格-销量等多个维度对销售数据进行深入分析。

## 2. 文件说明

| 文件名                | 类型       | 描述                                                                 |
| --------------------- | ---------- | -------------------------------------------------------------------- |
| sales_data.csv        | 数据集     | 包含 1000 条模拟电子产品销售记录的 CSV 文件。字段包括：OrderID (订单ID), Date (日期), Product (产品名称), Category (产品类别), City (销售城市), Quantity (数量), Price (单价), Sales (销售额)。 |
| data_analysis.py      | Python代码 | 核心分析脚本，使用 Pandas 进行数据处理，使用 Matplotlib 进行可视化。 |
| monthly_sales_trend.png | 结果图表   | 月度总销售额趋势图。                                                 |
| category_city_sales.png | 结果图表   | 产品类别和城市销售额对比图。                                         |
| price_quantity_scatter.png | 结果图表 | 产品平均价格与总销量关系散点图。                                     |

## 3. 环境准备

在运行分析代码之前，请确保您的 Python 环境中安装了以下库：

```bash
pip install pandas numpy matplotlib
```

## 4. 运行步骤

1. 下载文件：将所有附件（sales_data.csv 和 data_analysis.py）下载到同一个文件夹中。
2. 打开终端/命令行：导航到该文件夹。
3. 执行脚本：运行以下命令执行数据分析脚本：
   ```bash
   python data_analysis.py
   ```
4. 查看结果：脚本将在终端输出分析过程和关键统计结果，并生成三个 PNG 格式的图表文件到当前文件夹。

## 5. 分析要点（供学习参考）

data_analysis.py 脚本中包含了以下几个主要的分析模块：

### 模块一：数据加载与清洗
- 使用 `pd.read_csv()` 加载数据。
- 使用 `df.info()` 和 `df.head()` 进行初步检查。
- 使用 `pd.to_datetime()` 将日期列转换为正确的格式。

### 模块二：时间序列分析
- 通过 `df['Date'].dt.to_period('M')` 提取月份。
- 使用 `df.groupby('Month')['Sales'].sum()` 计算月度销售额。
- 使用 `plt.plot()` 绘制时间趋势图。

### 模块三：类别和地域分析
- 使用 `df.groupby('Category')['Sales'].sum()` 和 `df.groupby('City')['Sales'].sum()` 进行分组聚合。
- 使用 `plt.bar()` 绘制条形图进行对比。

### 模块四：进阶分析（价格与销量关系）
- 使用 `df.groupby('Product').agg(...)` 同时计算每个产品的平均价格 (`np.mean`) 和总销量 (`np.sum`)。
- 使用 `plt.scatter()` 绘制散点图，探索价格和销量之间的潜在关系。
=======

# 销售数据分析项目 (Sales Data Analysis Project)

这是一个基于 Python 的数据分析脚本，用于处理销售数据 CSV 文件。它能够自动执行数据清洗、描述性统计分析，并生成多维度的可视化图表，帮助快速洞察销售趋势、产品表现及地域分布。

## 📊 主要功能

1.  **数据加载与初步检查**
    * 自动加载 CSV 数据。
    * 显示数据形状、前 5 行预览及基本信息（Info）。
2.  **数据清洗与准备**
    * 自动转换日期格式。
    * 检查并报告缺失值情况。
3.  **描述性统计分析**
    * 计算数值列（销量、价格、销售额）的统计指标（均值、标准差、分位数等）。
    * 计算总销售额和平均订单价值 (AOV)。
4.  **可视化分析 (自动生成 PNG 图片)**
    * **月度趋势:** 生成折线图，展示随时间变化的销售走势。
    * **类别与城市分析:** 生成并排条形图，对比不同产品类别和城市的销售表现。
    * **价格与销量关系:** 生成散点/气泡图，分析产品定价与销量的相关性。
5.  **中文支持**
    * 内置 Matplotlib 中文配置 (`SimHei`)，解决图表乱码问题。

## 🛠️ 环境依赖

本项目基于 Python 开发，已经过以下库版本的测试：

* **NumPy:** 2.3.2
* **Pandas:** 2.3.2
* **Matplotlib:** 3.10.7

### 安装依赖

您可以使用 pip 安装所需的库（建议创建虚拟环境）：

```bash
pip install pandas numpy matplotlib
````

*注意：如果无法获取上述特定版本，安装最新稳定版通常也能兼容运行。*

## 📂 数据准备 (Input Data)

脚本默认读取 CSV 文件。为了确保脚本正常运行，您的 CSV 文件 (`sales_data.csv`) 应至少包含以下列名：

| 列名 (Column Name) | 数据类型 | 说明 |
| :--- | :--- | :--- |
| `Date` | String/Date | 订单日期 (例如: 2023-01-01) |
| `Sales` | Float/Int | 销售金额 |
| `Quantity` | Int | 销售数量 |
| `Price` | Float | 单价 |
| `Category` | String | 产品类别 |
| `City` | String | 销售城市 |
| `Product` | String | 产品名称 |

## ⚙️ 配置与运行

### 1\. 修改文件路径

在运行脚本之前，**必须**修改代码中的 `file_path` 变量，使其指向您电脑上 CSV 文件的实际位置。

打开 Python 脚本，找到以下行并修改：

```python
# 将此路径修改为您本地文件的相对路径
file_path = r"sales_data.csv"
```

### 2\. 运行脚本

在终端或 IDE 中运行脚本：

```bash
python sales_analysis.py
```

## 📈 输出结果 (Outputs)

脚本运行完成后，将在当前目录下生成以下文件：

1.  **控制台输出:** 包含数据概览、缺失值报告、统计摘要（总销售额、AOV等）。
2.  **可视化图表:**
      * `monthly_sales_trend.png`: 月度销售趋势图。
      * `category_city_sales.png`: 各类别与城市销售额对比图。
      * `price_quantity_scatter.png`: 产品平均价格与总销量关系散点图。

## 📝 注意事项

  * **字体问题:** 脚本默认使用 `SimHei` (黑体) 以支持中文显示。如果您在非 Windows 系统 (如 Mac/Linux) 上运行且未安装该字体，可能会遇到报错或乱码。此时需更改 `plt.rcParams['font.sans-serif']` 为系统支持的中文字体 (如 `Arial Unicode MS` 或 `Heiti TC`)。
  * **编码格式:** 请确保源 CSV 文件为 `UTF-8` 或 `GBK` 编码，以免读取时发生错误。

-----

*最后更新时间: 2025-11-24*



>>>>>>> 3d3cf4a (readmi update)
